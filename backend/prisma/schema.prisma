generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://ipl_database_6t6z_user:3rpBiv8DS6X5bhX3kiUXo0TUeLThobJQ@dpg-d73enqhr0fns73a9jnt0-a/ipl_database_6t6z")
}

model User {
  id         Int         @id @default(autoincrement())
  name       String
  email      String?     @unique
  mobile     String?     @unique
  password   String
  isAdmin    Boolean     @default(false)
  createdAt  DateTime    @default(now())
  selections Selection[]
}

model Match {
  id         Int         @id @default(autoincrement())
  matchDate  DateTime
  team1      String
  team2      String
  venue      String?
  winnerTeam String?
  isComplete Boolean     @default(false)
  selections Selection[]
}

model Selection {
  id           Int      @id @default(autoincrement())
  userId       Int
  matchId      Int
  selectedTeam String
  isCorrect    Boolean?
  createdAt    DateTime @default(now())
  user         User     @relation(fields: [userId], references: [id])
  match        Match    @relation(fields: [matchId], references: [id])

  @@unique([userId, matchId])
}